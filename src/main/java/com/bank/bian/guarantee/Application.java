package com.bank.bian.guarantee;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Guarantee
 *
 * Business Area:      Operations and Execution
 * Business Domain:    Loans and Deposits
 * Functional Pattern: Fulfill
 * Asset Type:         Guarantee Facility
 * Control Record:     Guarantee Facility Fulfillment Arrangement
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Guarantee — BIAN Semantic API")
                .description("Service Domain 'Guarantee' (Fulfill pattern) "
                        + "operating on the 'Guarantee Facility Fulfillment Arrangement' control record.")
                .version("0.1.0"));
    }
}
